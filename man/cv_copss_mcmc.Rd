% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_copss.R
\name{cv_copss_mcmc}
\alias{cv_copss_mcmc}
\title{MCMC-based grid search CV for getModel}
\usage{
cv_copss_mcmc(
  y,
  taus,
  H,
  X = NULL,
  w,
  grid,
  base_args = list(),
  mcmc_warmup = 200,
  mcmc_draws = 300,
  seed = 123,
  verbose = TRUE
)
}
\arguments{
\item{y}{Numeric vector of responses.}

\item{taus}{Numeric vector of quantile levels.}

\item{H, X}{Design matrices.}

\item{w}{Integer warm-up period.}

\item{grid}{data.frame with columns for hyperparameters (lambda_nc, eps_rel, etc.)}

\item{base_args}{Named list of additional arguments passed to getModel.}

\item{mcmc_warmup}{Number of MCMC warmup iterations (default 200).}

\item{mcmc_draws}{Number of MCMC sampling iterations (default 300).}

\item{seed}{Random seed.}

\item{verbose}{Print progress.}
}
\value{
data.frame with grid and CV losses.
}
\description{
Uses MCMC with shorter draws instead of MAP for hyperparameter selection.
This can improve results when MAP estimation is unreliable.
}
\examples{
\donttest{
set.seed(123)
n <- 200
y <- rnorm(n)
taus <- c(0.25, 0.5, 0.75)
H <- getSustainedShift(n, l = 20, w = 30)
grid <- data.frame(lambda_nc = c(2, 5))
cv_result <- cv_copss_mcmc(y = y, taus = taus, H = H, w = 30, grid = grid,
                            mcmc_warmup = 100, mcmc_draws = 100)
}

}
