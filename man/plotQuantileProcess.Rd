% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotBQQ.R
\name{plotQuantileProcess}
\alias{plotQuantileProcess}
\title{Plot the data process with fitted quantile bands over time}
\usage{
plotQuantileProcess(
  fit,
  time = NULL,
  taus = NULL,
  center = 0,
  scale = 1,
  detection = NULL,
  alpha = 0.05,
  y = NULL,
  title = NULL
)
}
\arguments{
\item{fit}{A MAP fit from \code{getModel()}.}

\item{time}{Optional x-axis vector (default \code{seq_len(n)}).}

\item{taus}{Quantile levels (default recovered from \code{fit}).}

\item{center, scale}{Map the fit-scale quantiles/data back to the display scale
as \code{value * scale + center} (e.g. the standardization used before fitting).}

\item{detection}{Optional \code{detectChangepoints_gamma()} result; adds onset
lines and localized-change-point markers.}

\item{alpha}{Significance level for the block decision (default 0.05).}

\item{y}{Optional observed series override (default \code{fit$y}).}

\item{title}{Optional plot title.}
}
\value{
A ggplot object.
}
\description{
Graph type 1: the observations with the five fitted quantile bands, and
(optionally) crimson onset lines and within-block localized change points
from a \code{detectChangepoints_gamma()} result.
}
