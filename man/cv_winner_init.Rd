% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_copss.R
\name{cv_winner_init}
\alias{cv_winner_init}
\title{Starting values for the final fit from a cross-validation winner}
\usage{
cv_winner_init(cv, k = 1L)
}
\arguments{
\item{cv}{Result of \code{cv_copss_grid}.}

\item{k}{Grid row (in the sorted result); default 1, the winner.}
}
\value{
A named list of starting values, or \code{NULL} when the fold fits were
  not recorded.
}
\description{
Averages the two fold MAP fits of grid row \code{k} of a \code{cv_copss_grid}
result (row 1 is the winner) and returns them as a list \code{beta0},
\code{betaX}, \code{gamma} for \code{getModel(map_init_values = )}. The folds
use the same design as the full series, so the averaged coefficients are on the
scale of the final fit and start the optimizer close to its solution.
}
