% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotBQQ.R
\name{plotQSSProcess}
\alias{plotQSSProcess}
\title{Plot the quantile-shape-statistic (QSS) process over time}
\usage{
plotQSSProcess(
  fit,
  eta = NULL,
  H = NULL,
  X = NULL,
  time = NULL,
  center = 0,
  scale = 1,
  level = 0.95,
  detection = NULL,
  seed = NULL,
  title = NULL,
  xlab = "time",
  ylab = NULL,
  show_onset = TRUE,
  show_located = TRUE
)
}
\arguments{
\item{fit}{A MAP fit from \code{getModel()}.}

\item{eta}{Optional posterior predictive-quantile array from \code{getEta()};
computed internally if not supplied.}

\item{H, X}{Optional design matrices passed to \code{getEta()} (default from fit).}

\item{time}{Optional x-axis vector (default \code{seq_len(n)}).}

\item{center, scale}{Map the fit-scale quantiles to the display scale.}

\item{level}{Credible-band level (default 0.95).}

\item{detection}{Optional \code{detectChangepoints_gamma()} result. Its recorded
\code{basis} and \code{statistic} select which block test flags the blocks, and
its \code{detected_blocks} supplies both the block onset (dashed line) and the
localized change-point \code{signal_obs} (solid line), the latter obtained under
whichever \code{signal_position} was passed to \code{detectChangepoints_gamma()}.}

\item{seed}{Optional seed for \code{getEta()}.}

\item{title}{Optional plot title.}

\item{xlab}{Label for the x axis (default \code{"time"}).}

\item{ylab}{Label for the shared y axis. Default \code{NULL} (no label), since
the four panels are already named by their facet strips and each has its own
free scale.}

\item{show_onset, show_located}{Logical display toggles (default TRUE): draw
the block-onset marks and the localized change-points recorded in
\code{detection}. They only hide layers; the decisions themselves are
made (and recorded) by \code{detectChangepoints_gamma()}.}
}
\value{
A ggplot object (four stacked, free-y facets).
}
\description{
Graph type 2: posterior Location, Scale, Skewness and Kurtosis over time, each
as a median line with a credible-band ribbon. Posterior draws are sorted within
each draw (non-crossing) before the shape statistics are formed, so the ratios
stay well defined.
}
