% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotBQQ.R
\name{plotBQQSummary}
\alias{plotBQQSummary}
\title{Summary figure: quantile process, shape profile and shift heatmap}
\usage{
plotBQQSummary(
  fit,
  detection,
  time = NULL,
  basis = c("lmom", "qss"),
  eta = NULL,
  H = NULL,
  X = NULL,
  level = 0.95,
  comparator = NULL,
  comparator_label = "Comparator",
  proposed_label = "Proposed (BQQ)",
  match_tol = NULL,
  ylab = "value",
  block_labels = NULL,
  label_every = NULL,
  title = NULL,
  widths = c(1.05, 1),
  heights = c(1, 0.8),
  date_breaks = "12 months",
  date_labels = "\%Y",
  seed = NULL
)
}
\arguments{
\item{fit}{A MAP fit from \code{getModel()}.}

\item{detection}{A \code{detectChangepoints_gamma()} result whose \code{basis}
includes the requested shape basis.}

\item{time}{Optional x-axis vector (default \code{seq_len(n)}). A \code{Date}
vector gets yearly breaks with rotated labels (see \code{date_breaks}).}

\item{basis}{The shape basis shown on the right and used for the change-point
marks: \code{"lmom"} (default) or \code{"qss"}.}

\item{eta, H, X}{Passed to the profile panel; see \code{\link{plotQSSProcess}}.}

\item{level}{Credible-band level of the profile (default 0.95).}

\item{comparator}{Optional integer vector of change-point positions
(observation indices) from a comparator method, e.g.
\code{changepoint::cpts()} of a binary-segmentation fit.}

\item{comparator_label, proposed_label}{Legend labels of the comparator and of
the proposed method.}

\item{match_tol}{A proposed and a comparator change-point within this many
observations of each other are drawn once, at the proposed location, as
"Both". Default \code{NULL}: the block length recorded in \code{detection},
since BQQ resolves a change only to its block.}

\item{ylab}{Label for the y axis (default \code{"value"}). Set it to name the
series actually plotted, e.g. \code{"residual"} when the model was fitted to
regression residuals, or \code{"difference"} when it was fitted to their
first-order differences. Use \code{NULL} to drop the label.}

\item{block_labels}{Labels of the heatmap columns. Default \code{NULL}: the
block start dates when \code{time} is a \code{Date} vector, else the block
start indices.}

\item{label_every}{Passed to \code{\link{plotGammaHeatmap}}.}

\item{title}{Optional plot title.}

\item{widths, heights}{Relative widths of the left and right columns, and
relative heights of the profile and heatmap panels.}

\item{date_breaks, date_labels}{Breaks and label format of the x axis when
\code{time} is a \code{Date} vector.}

\item{seed}{Optional seed for \code{getEta()}.}
}
\value{
A \pkg{patchwork} object. The talk used 12.6 by 5.5 inches at 200 dpi.
}
\description{
The three-panel layout of the ARCOS illustration in the JSM 2026 talk. Left:
the data with the fitted quantile bands and the localized change-points
(\code{\link{plotQuantileProcess}}, with the comparator overlay when
\code{comparator} is given). Right: the shape profile of one basis
(\code{\link{plotLmomProcess}} or \code{\link{plotQSSProcess}}, bands only)
above that basis's shift heatmap (\code{\link{plotGammaHeatmap}}). All three
panels read the same \code{detection} object, so the circles, the bordered
blocks and the heatmap subtitle follow one decision rule.
}
